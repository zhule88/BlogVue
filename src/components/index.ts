//import { App } from 'vue';
//const components = import.meta.glob('./components/**/*.vue');
/*
export default {
    install(app: App) {
        Object.keys(components).forEach(async (key) => {
            const component = await components[key]() as any;
            const componentName = key.split('/').pop()?.replace('.vue', '') as string;
            app.component(componentName , component.default );
        });
    }
}; */
